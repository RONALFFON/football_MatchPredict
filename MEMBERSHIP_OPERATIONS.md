# 会员 P0 操作与上线说明

## 产品规则

- 经典、彩票的简化计算及结果保存免费。
- 免费账号每天有 3 次 AI 额度：批量预测每场计 1 次，Agent 每个有有效文本的回答计 1 次。
- 有效会员不限次数；这不是模型费用不限预算，调用限流和成本预算将在 P1 完成。
- 月度／年度会员分别为 1／12 个自然月，不设价格，不接支付。
- 有效期内续期从原到期时刻顺延；已经过期或撤销则从当前时间重新开始。
- 月末顺延时取目标月份最后一个有效日期，例如 1 月 31 日续 1 个月到 2 月 28 日（闰年 29 日）。
- 冻结只暂停会员权益，不停用登录，不暂停到期时间；冻结用户按免费额度执行。解冻时已过期则仍显示过期。
- 旧数据中 `premium` 且到期时间为空的账号保留长期会员；新开通不提供长期会员选项。
- 人工开通不表示已收款，系统不生成付款成功记录。

## 无需迁移即可使用

- 登录后点击导航用户名进入 `/account`，查看权益、重置时间及已保存预测。
- 经典保存由后端重新计算结果；新前端为每次分析生成请求编号，重复保存只保留第一次。旧前端缺少编号时，同一用户同日同内容去重。
- AI 成功结果携带签名保存凭证。凭证只用于本人保存对应内容，不可登录、跨用户保存或修改分析内容；有效期沿用 JWT_EXPIRE_HOURS。过期后需重新分析获取新凭证。
- AI 保存不额外计次。分析结果目前仍需用户点击保存，尚未自动建立完整的预测/用量账本。

## 会员管理启用条件

1. 人工确认并授权后执行 `backend/sql/membership_p0.sql`。本轮只生成脚本，没有执行 DDL，也没有修改生产会员。
2. 系统管理员安全默认邮箱是 `admin@matchpredict.example`，可用服务端 `SYSTEM_ADMIN_EMAIL` 修改为实际管理员邮箱。系统只对数据库中已经存在且邮箱匹配的账号授予权限，邮箱匹配不区分大小写。该保留邮箱不能通过公开注册创建，防止未验证邮箱被抢占。
3. 旧的 `ADMIN_USER_IDS` 白名单继续兼容，建议管理员邮箱账号确认可用后清空。系统管理员身份和会员身份相互独立。
4. `BUSINESS_TIMEZONE` 默认 `Asia/Shanghai`，同时用于 Python 到期判断和 PostgreSQL 连接会话。既有无时区日期按这个时区解释，上线前核实原始数据含义；不要直接切换时区而忽略历史记录。
5. 重启后端。系统管理员会看到“系统管理”导航和 `/admin` 页面，个人中心同时显示会员管理表单。

迁移增加 `users.membership_state` 与 `membership_events` 审计表，不会给任何用户自动开通会员。会员读取兼容迁移前结构；管理接口在迁移缺失时返回 503，默认拒绝变更。若发生结构漂移或部分迁移，事务错误也不能绕过审计写入。

## 管理流程与权限

- 输入目标用户名、选择开通/续期、冻结、解冻或撤销，并填写原因。
- 开通/续期必须选择月度或年度方案；其他操作不能传套餐。
- 每次操作需确认，携带 UUID 请求编号。网络失败后保持原编号重试，避免重复延长会员。
- 同一管理员重用请求编号但变更操作内容，接口返回 409；正常重放返回第一次结果。
- 用户行锁串行化同一用户的变更；权益更新与审计新增在同一事务提交，审计失败会回滚权益更新。
- 页面可查询目标用户最近 50 条变更，包含操作人 ID、操作类型、原因、变更前后值与时间。
- 普通用户与普通会员看不到系统管理入口，前端路由会拒绝打开 `/admin`，后端接口仍会独立执行最终权限校验。
- 系统管理员专属能力包括会员管理、会员审计、系统状态概览和体彩实时刷新。管理员邮箱和其他敏感配置不会通过概览接口返回。

接口：

- `GET /api/v1/account?offset=0`：本人的权益与分页保存记录。
- `POST /api/v1/admin/memberships/{username}`：会员操作。
- `GET /api/v1/admin/memberships/{username}/history`：管理员查询最近审计。

## 当前验证范围与上线检查

本地测试使用隔离假数据库和禁止外部调用的夹具，覆盖月末与闰年续期、时区边界、权限隔离、签名凭证、保存幂等、重复会员请求以及模拟审计失败回滚；前端验证组件编译和失败重试沿用原请求编号。尚未执行真实 PostgreSQL 并发与迁移测试。

上线前还需核实 users 主键/时间字段类型、应用数据库角色的最小写权限，以及既有登录入口对升级密码格式的支持。任何迁移执行、真实会员开通或真实支付接入都不属于本轮已经执行的操作。

P1 继续处理持久化用量账本、模型请求幂等、进程崩溃补偿、限流和成本告警；P2 支付接入按用户要求保持暂停。
