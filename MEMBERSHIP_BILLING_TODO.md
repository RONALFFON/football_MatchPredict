# 会员与计费后续任务

更新日期：2026-09-09。范围：当前 frontend/ + backend/ 主链路。

## 本轮进度：P0 已实现，会员管理待迁移后启用

产品方向已确认：月度／年度会员，暂不接真实支付。

- 已实现统一权益规则、业务时区、个人中心、保存不扣次、保存去重和 AI 保存凭证校验。
- 已实现后台月度／年度开通续期、冻结、解冻、撤销、系统管理员邮箱、兼容 ID 白名单以及同事务审计；新增字段和审计表的迁移脚本为 `backend/sql/membership_p0.sql`，尚未执行任何 DDL。
- 安全默认系统管理员邮箱为 `admin@matchpredict.example`，只能匹配预先存在的账号，公众注册不能占用；部署时应通过 `SYSTEM_ADMIN_EMAIL` 修改为实际管理员邮箱。
- 已加入管理员专属导航、`/admin` 路由守卫、系统概览和体彩实时刷新权限。审计结构缺失时会员变更接口返回 503，不能跳过审计单独开通。
- 前台会员状态兼容迁移前的 users 表；新增字段通过 `to_jsonb(users)` 读取，字段不存在时按旧数据解释。
- 个人中心展示的是已保存预测，不是完整用量账本。P1 的模型调用幂等、崩溃恢复、成本与限流仍未实现。
- 操作与上线说明见 `MEMBERSHIP_OPERATIONS.md`。

## 当前已经具备的机制

- 注册用户默认写入 `users.user_type = free`。
- `premium` 且会员未过期时享有不限次数；`membership_expires` 为空的既有会员按长期会员兼容。过期会员按免费额度处理，用户接口也展示为免费用户。
- 免费用户每天 3 次，额度日期与会员到期判断统一使用 `BUSINESS_TIMEZONE`（默认 Asia/Shanghai），数据库会话同步设置该时区。
- 五大联赛 AI 按每场比赛预占一次，失败场次释放额度；单批最多 20 场。
- 英超 Agent 每次对话预占一次，无有效文本时释放；已经产生有效文本的对话计一次。
- 经典与彩票简化预测免费；保存任何模式不再扣 AI 次数。经典结果由后端重算，AI 结果只接受本账号分析返回的保存凭证。相同保存请求只保留第一条记录。
- 密码使用带随机盐的 PBKDF2-SHA256（600,000 次迭代）；旧 SHA-256 密码验证成功后升级。参数参考 [OWASP Password Storage Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html)。

## 当前尚未具备的机制

现已实现管理员会员操作入口与权限控制；会员购买、支付订单、支付回调、退款或对账尚未实现。`premium` 是现存数据中的身份标识，不代表系统已经完成付费订阅流程。不能把次数扣减表述为收款成功。

## P0：先明确权益与状态

- [x] 统一规则：免费用户每日 3 次 AI，会员有效期内不限次，经典与彩票简化计算及保存免费。
- [x] 预测与保存分离计次；AI 凭证绑定用户且不可充当登录令牌，重复保存不重复入库或扣次。
- [x] 新开通只支持 1 或 12 个自然月；保留已有长期会员，禁止通过续期误缩短长期会员有效期。
- [x] 实现普通、有效、过期、长期、冻结、撤销状态，以及续期与提前续期；冻结期间有效期照常流逝，撤销后可重新开通。退款联动保留在 P2。
- [x] 统一业务时区；无时区历史时间按业务时区解释，会员在到期瞬间失效。生产数据原始时区仍需上线前核实。
- [x] 已实现管理员 ID 白名单、会员操作与审计代码、操作幂等和前台管理表单；实际启用依赖尚未执行的迁移。
- [x] 个人中心展示权益、到期时间、额度重置时间与保存记录，支持分页。
- [ ] P1 完成后，在个人中心补充逐次 AI 用量账单。

验收：普通用户不能自行成为会员；到期后各入口权限一致；续费不丢失已有剩余时长；会员调整可以审计。

## P1：用量账本与成本控制

- [ ] 设计持久化用量记录，包含请求 ID、用户、比赛、模式、模型、预占数量、最终数量及状态。
- [ ] 实现预占、成功结算、失败释放和过期预占回收，支持进程崩溃及数据库暂时不可用后的补偿。
- [ ] 加入用户级请求幂等键，重复点击、网络重试和重复保存不产生重复计次。
- [ ] 记录模型 token、耗时、失败原因和估算成本；模型成本与向用户收取的金额分别记录。
- [ ] 为免费和会员用户分别设置调用速率、并发数与成本上限。当前“会员不限次数”不等于模型成本无限。
- [ ] 增加额度退款失败告警、用量对账和人工纠错入口。

验收：并发请求无法超额；重复请求不重复扣次；失败和中断有可追踪结果；跨日退款不影响新一天额度；服务重启后可恢复悬挂记录。

## P2：支付与订阅流程

- [x] 确认采用月度／年度会员，暂不接真实支付。
- [ ] 开通真实支付前再确认套餐价格和币种。
- [ ] 选择支付渠道并配置测试环境，凭据只保存在服务端环境配置中。
- [ ] 定义套餐版本、订单状态、金额最小单位、支付流水和会员权益关联。
- [ ] 由服务端根据套餐计算金额，客户端不能决定价格或直接标记支付成功。
- [ ] 验证支付回调签名、订单、金额与币种，处理重复回调、乱序回调、超时和主动查单。
- [ ] 支付确认和权益发放建立一致性机制，权益只发放一次。
- [ ] 实现退款、关闭订单、取消续订和权益回收；若支持自动续费，提供明确的授权及取消入口。
- [ ] 增加支付对账、异常订单告警，以及客服查询和处理入口。

验收：未付款不发权益；伪造或重复回调不会开通会员；付款后发放失败可补偿；退款后订单与权益一致。

## P3：上线与回归

- [ ] 确认生产 `password_hash` 字段支持新格式长度，且所有仍在线的认证入口支持升级后的哈希。遗留 Flask 认证仍使用 SHA-256，不能在共用用户表时继续假定它与升级后账号兼容。
- [ ] 使用隔离环境验证 PostgreSQL 实际并发扣次、跨日释放、事务回滚和过期会员判断。本轮测试未连接真实数据库。
- [ ] 支付仅先在渠道测试环境验证；开通真实收款前单独确认商户、价格和上线安排。
- [ ] 所有新增表、字段、索引和迁移脚本先形成可评审方案；未获明确命令不执行 DDL。
- [ ] 相关测试文件只在本地验证，不加入提交；推送分支时再按项目约定运行 typecheck / lint。

## 本轮修复之外的架构任务

- [ ] 统一数据库连接池、超时、错误状态码和请求日志。
- [ ] 完善 AI 批量任务进度、端到端截止时间、SSE 取消及独立域名部署。
- [ ] 用球队 ID、明确赛季和统计截止时间替代模糊名字匹配；目前英超统计和积分榜默认使用各自表内最新赛季。
- [ ] 持久化赔率市场类型与采集时间；历史记录的市场含义不明确时重新采集，不能静默套用普通胜平负模型。
- [ ] 服务端生成可追溯预测记录、赔率快照和模型版本，建立开赛前预测的历史评估流程。
