# Codex 项目工作规则

## 编码与乱码防护

- 所有新建或修改的源代码、配置、模板、文档、JSON、CSV 和日志文本统一使用 UTF-8（无 BOM）；禁止使用 GBK、GB18030、Big5 或系统 ANSI 编码。
- 文本文件统一使用 LF 换行和 UTF-8 字节序列。不要让编辑器自动猜测编码，也不要在不确认原始编码的情况下批量转换文件。
- Python 读写文本文件时显式指定 `encoding="utf-8"`；写 JSON 时使用 `ensure_ascii=False`，保留中文可读性。二进制文件必须使用 `rb`/`wb`。
- HTML 模板必须保留 `<meta charset="UTF-8">`；接口返回的 JSON 和日志内容必须保持 Unicode，不得用 Unicode 替换字符（`U+FFFD`）代替原文。

## 修改后的校验

- 修改文本文件后，确认其 MIME 编码为 UTF-8，并用 `iconv -f UTF-8` 验证可以完整解码。
- 检查修改内容中没有 Unicode 替换字符（`U+FFFD`）或明显的乱码串；发现疑似乱码时先读取原始字节并识别编码，再决定是否修复。
- 生成或修改代码后运行与改动相关的语法检查或测试；不要为了“修复乱码”重写无关文件。

## 本地环境文件与远端提交

- `.env`、`.env.*`（保留 `.env.example`）、`.venv/`、`venv/` 和 `env/` 等本地环境文件及虚拟环境目录只供本地使用，禁止加入 Git、提交或推送到远端。
- 本地环境文件中可能包含密钥、数据库凭据和令牌；不得将其中的敏感信息复制到源代码、日志、文档或远端仓库。新增环境变量时只更新 `.env.example` 中的变量名和说明，不填写真实值。
- 提交或推送前必须检查 `git status` 和 `git diff --cached`，确认没有环境文件、虚拟环境目录或敏感配置。

## 交互约定

- 默认使用中文说明；代码、命令、文件名和错误信息保持原样，不要把可执行代码翻译成中文。
